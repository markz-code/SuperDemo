<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zpl">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".jetpack.JetpackDemoActivity"/>
        <activity android:name=".sqlite.SqliteActivity" />
        <activity android:name=".costomeview.ScaleImageViewActivity" />
        <activity android:name=".costomeview.MaterialEditextActivity" />
        <activity android:name=".costomeview.CustomeViewActivity" />
        <activity
            android:name=".animation.PathMeasureActivity"
            android:label="PathMeasure" />
        <activity
            android:name=".animation.AnimationActivity"
            android:label="Animation" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".animation.VectorDemo" />
        <activity android:name=".animation.MainLActivity" />
        <activity android:name=".animation.BezierMainActivity" />
        <activity android:name=".animation.SecondBezierActivity" />
        <activity android:name=".animation.ThirdBezierActivity" />
        <activity android:name=".animation.DrawPadActivity" />
        <activity android:name=".animation.PathMorthingActivity" />
        <activity android:name=".animation.WaveActivity" />
        <activity android:name=".animation.PathBezierActivity" />
    </application>

</manifest>